# Path to devices.json inventory file (recommended)
CISCO_DEVICES_JSON=/path/to/devices.json

# OR single device via env vars (fallback)
# CISCO_HOST=192.168.0.1
# CISCO_USER=admin
# CISCO_PASS=changeme
# CISCO_PORT=22
